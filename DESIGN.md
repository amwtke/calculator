# 设计文档

## 架构设计
采用经典的编译器设计模式：
1. **词法分析** (TokenParser) - 将字符串转换为Token流
2. **语法分析** (ASTParser) - 构建抽象语法树(AST)
3. **递归求值** (ASTNode) - 遍历AST计算结果

## 核心类设计
- `Calculator`: 主入口，协调整个计算流程
- `TokenParser`: 词法分析器
- `ASTParser`: 语法分析器，使用递归下降解析
- `ASTNode`: 语法树节点接口
- `NumberASTNode`, `OpASTNode`: 具体节点实现

## 错误处理策略
- 语法错误：在解析阶段检测
- 运行时错误：在求值阶段检测（如除零）
- 文件错误：IO操作异常处理

## 扩展性考虑
- 接口设计便于添加新运算符
- 模块化结构支持功能扩展